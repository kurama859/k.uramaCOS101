fn average(nums: &[f64]) -> f64 {
    let sum: f64 = nums.iter().sum();
    sum / nums.len() as f64
}
fn main() {
	let t = 450000.00; let repeated_t = [t; 2];
	let m = 1500000.00;
	let h = 750000.00;  let repeated_h = [h; 3];
	let d = 2850000.00; let repeated_d = [d; 3];
	let a = 250000.00;

	// average i hope this works, according to research,this is just combining my stuff because it is too much to handle omooo.Vec = Vector = Rust's resizable list/array.
//mut = we need to be able to change it.
//So this makes an empty list called data. Right now it's []

	let mut data = Vec::new();
    data.extend(repeated_t);
    data.extend(repeated_h);
    data.extend(repeated_d);
    data.push(m);
    data.push(a);

    println! ("Average is {}",
    average(&data));


}